# 性能问题分析报告

## 🔴 严重问题

### 1. **重复添加 script 标签到 DOM**
- **位置**: `src/main.js:92`
- **问题**: 每次页面刷新，`loadScript()` 都会向 `document.body` 添加新的 script 标签，旧的从不清理
- **影响**: DOM 节点不断累积，内存占用持续增长
- **证据**: 代码中 `document.body.appendChild(script)` 没有清理机制

### 2. **重复添加 visibilitychange 事件监听器**
- **位置**: `src/js/plants/grass.js:198-211`
- **问题**: 每次调用 `createGrass()` 都会添加新的 `visibilitychange` 监听器，从不清理
- **影响**: 多个监听器同时存在，导致重复执行和内存泄漏

### 3. **Phaser.Game 实例没有清理机制**
- **位置**: `src/js/scenes/game.js:17`
- **问题**: 每次页面刷新都创建新的 Phaser.Game 实例，旧的可能没有完全释放
- **影响**: WebGL 上下文和游戏对象没有被清理

### 4. **对象频繁创建和销毁**
- **位置**: 所有植物模块
- **问题**: 植物对象频繁创建和销毁，没有对象池
- **影响**: 产生大量临时对象，频繁触发 GC

## 🟡 中等问题

### 5. **定时器没有统一管理**
- **位置**: 各模块
- **问题**: 多个定时器没有统一管理和清理机制
- **影响**: 页面隐藏时定时器可能还在运行

### 6. **GameData 频繁调用 localStorage**
- **位置**: `src/js/core/gameData.js`
- **问题**: 每次更新数据都立即调用 localStorage
- **影响**: 可能导致性能问题（localStorage 是同步操作）

## 📊 性能影响

1. **内存占用**: 持续增长，不会下降
2. **GC 标记**: 频繁触发，导致卡顿
3. **DOM 节点**: 不断增加
4. **事件监听器**: 不断累积

## 🛠️ 修复建议

### 高优先级
1. ✅ 修复 script 标签重复添加问题（使用标记避免重复）
2. ✅ 修复 visibilitychange 监听器重复添加问题（添加一次并持久化）
3. ✅ 添加 Phaser.Game 单例管理

### 中优先级
4. 优化对象创建（可选，对当前游戏规模影响不大）
5. 添加清理机制
6. 优化 GameData 的 localStorage 写入（防抖或批量）
